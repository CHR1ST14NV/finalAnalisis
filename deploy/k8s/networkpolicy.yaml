apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: chan-web-allow
spec:
  podSelector:
    matchLabels:
      app: chan-web
  policyTypes:
  - Ingress
  - Egress
  ingress:
  - from:
    - podSelector: {}
  egress:
  - to:
    - podSelector: {}

